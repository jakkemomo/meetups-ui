// import { ContactCard } from "@/entities/chat/chatContact";
// import { ChatDetails } from "@/entities/chat/model/types";

// export const getChatsList = (chats: ChatDetails[]) => {
//   const chatsList = chats.map((chat) => <ContactCard key={chat.id} data={chat} />)

//   return chatsList;
// }
